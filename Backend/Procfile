web: node server.js
```

---

## 🔧 **Vérifie aussi que tous les fichiers sont bien dans le repo**

Assure-toi que Railway a accès à :
- ✅ `server.js`
- ✅ `app.js`
- ✅ `package.json`
- ✅ Tous les dossiers (`models/`, `routes/`, `controllers/`, `middlewares/`)

---

## 🎯 **Action immédiate : Vérifie les Deploy Logs Railway**

1. Va sur Railway Dashboard
2. Clique sur ton projet backend
3. Va dans **Deployments**
4. Clique sur le dernier déploiement
5. Regarde les **Build Logs** et **Deploy Logs**

**Copie-moi les logs complets, surtout les lignes d'erreur en rouge !**

---

## 📋 **Alternative : Redémarre depuis zéro sur Railway**

Si rien ne fonctionne :

1. **Delete** le projet actuel sur Railway
2. **Create New Project**
3. Connecte ton repo GitHub
4. Ajoute les variables d'environnement :
```
   NODE_ENV=production
   TOKEN_SECRET=gwEtS=KfKfR^zxJP83ULiw
   CORS_ORIGIN=https://front-bljs483n6-guerrouis-projects-7afb0b61.vercel.app